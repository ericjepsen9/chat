# 项目 Bug 梳理与分析（安全 / 性能 / 逻辑）

本文基于当前代码做静态审查与基础可运行验证，按「安全漏洞」「性能问题」「逻辑错误」分类整理。

## 一、安全漏洞

### 1) `/api/admin/dashboard` 缺少管理员权限校验（高危）
- **现象**：只要是任意已登录用户，就可访问全平台统计、订单、用户列表、商品列表等后台数据。
- **证据**：接口仅调用 `requireAuth`，未判断角色/权限位。
- **风险**：普通用户可直接读取后台敏感运营数据，造成越权访问与信息泄露。
- **建议**：增加 `role` 字段（如 `admin`），并在后台接口加 `requireAdmin` 中间逻辑；同时考虑按字段最小化返回数据。

### 2) `/api/orders` 可通过 `userId` 参数越权读取他人订单（高危）
- **现象**：`GET /api/orders?userId=<任意用户ID>` 时，后端按传入 `userId` 过滤订单，但没有校验 `userId === authUser.id`。
- **风险**：攻击者可遍历用户 ID 查询他人订单信息（买家/卖家关系、金额、商品摘要）。
- **建议**：忽略外部 `userId` 参数，统一使用 `authUser.id`；若需要管理员查询，必须走管理员专用接口。

### 3) 登录接口无频控/锁定策略，存在暴力破解风险（中危）
- **现象**：`/api/login` 校验密码后直接返回，无 IP/账号维度限速、失败计数、验证码策略。
- **风险**：可被批量尝试弱口令。
- **建议**：增加令牌桶限流、失败次数退避、可选验证码和审计告警。

### 4) 会话令牌长期有效且仅保存在内存（中危）
- **现象**：`sessions` 为进程内 `Map`，发放后无 TTL、无轮换、无登出失效机制。
- **风险**：令牌一旦泄露可长期使用（直到服务重启）；同时可能造成会话表增长。
- **建议**：增加会话过期时间、刷新机制和显式登出；生产建议切换到可持久化且可撤销的会话存储。

### 5) Token 放在 URL 查询串用于 SSE（中危）
- **现象**：前端通过 `EventSource('/api/events?token=...')` 传 token；服务端也接受 query token。
- **风险**：URL 可能进入代理/日志/历史记录，泄露认证信息。
- **建议**：优先使用 Cookie（HttpOnly/Secure/SameSite）或短期一次性 SSE 凭证；至少避免长期主 token 出现在 URL。

### 6) 管理端大量 `innerHTML` 渲染未转义文本（中危）
- **现象**：后台页面将订单摘要、用户名、商品名、风险文案等直接拼接到 `innerHTML`。
- **风险**：若上游字段可控，可能触发存储型/反射型 XSS。
- **建议**：统一改用 `textContent` 或模板转义函数；禁用内联 HTML 拼接渲染用户输入。

## 二、性能问题

### 7) 密码哈希/校验使用同步 `scryptSync`，阻塞事件循环（中危）
- **现象**：注册/登录路径直接调用同步哈希 API。
- **影响**：并发登录/注册时会阻塞 Node 主线程，放大接口延迟。
- **建议**：切换异步 `crypto.scrypt`（Promise 化）或放到 worker 线程池。

### 8) WAL 与截断使用同步文件 I/O（中危）
- **现象**：`appendWal`、`truncateWalIfLarge` 使用 `appendFileSync/statSync/openSync/readSync/writeFileSync`。
- **影响**：高频写入时阻塞主线程，影响接口尾延迟。
- **建议**：改为异步流式写入；可按批次缓冲后落盘。

### 9) 管理看板统计存在 O(U×O) 级重复遍历（中低）
- **现象**：`userList.map` 中每个用户都对 `orders` / `friendships` 做 `filter` 计数。
- **影响**：用户和订单规模上来后，后台接口耗时明显增长。
- **建议**：先单次遍历构建 `Map<userId, count>` 聚合，再 O(U) 回填。

## 三、逻辑错误

### 10) 广播接口引用了不存在的索引字段，导致功能不可用（高）
- **现象**：`/api/conversations/:id/broadcast` 使用 `index.conversationsById`，但索引定义的是 `index.convById`。
- **连带问题**：同时调用了未定义的 `canAccessConversation`，即使修正字段名仍会抛错。
- **结果**：广播接口实际不可用（永远 404 或潜在报错）。
- **建议**：统一使用 `index.convById`，并补全 `canAccessConversation` 或改为已有成员校验逻辑。

### 11) 黑名单统计口径错误，后台风控数据失真（中）
- **现象**：后台统计用 `friendships.status === 'blacklisted'` 计算黑名单；而黑名单真实数据存于 `user.blacklist` 数组。
- **结果**：看板黑名单关系常为 0，风控提醒与实际不一致。
- **建议**：按 `users[].blacklist` 聚合统计，并明确“单向拉黑关系”的计数口径。

### 12) 管理端 token 读取与主站存储协议不一致（中）
- **现象**：管理端读取 `localStorage.token`；主站登录保存的是 `SESSION_KEY` 下 JSON（含 `token` 字段）。
- **结果**：用户正常登录后直接打开管理页，常提示“未检测到登录态”。
- **建议**：统一会话存储协议（复用主站 `SESSION_KEY` 解析逻辑）。

---

## 优先级建议（先修复）
1. **P0**：越权读取（admin dashboard、orders userId）。
2. **P1**：广播功能不可用（字段/函数引用错误）。
3. **P1**：XSS 风险点（admin_console `innerHTML`）。
4. **P2**：会话安全（TTL、失效、query token）。
5. **P2**：同步 CPU/I/O 阻塞与后台聚合性能。

## 已执行验证
- 启动服务并运行烟测脚本：`npm test`（在同一 shell 中拉起服务后执行）通过。
- 本文以静态代码审查为主，未做渗透式 PoC 注入；建议后续补自动化安全测试。
