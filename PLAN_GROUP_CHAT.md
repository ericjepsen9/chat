# 群聊功能设计方案

## 一、数据模型变更

### 1. Conversation 表扩展
在现有 conversation 模型中新增 `type: 'group'`，并增加以下字段：

```js
{
  id: 'conv_xxx',
  type: 'group',              // 新增类型
  name: '群聊名称',            // 群名称
  avatarUrl: '',              // 群头像（可选，默认九宫格拼图）
  ownerId: 'user_xxx',        // 群主
  admins: ['user_xxx'],        // 管理员列表
  members: ['user_1', 'user_2', ...],  // 成员列表
  memberNicknames: { 'user_1': '昵称' },  // 群内昵称
  announcement: '',            // 群公告
  inviteConfirm: false,        // 邀请确认（群主/管理员审批）
  mutedMembers: ['user_x'],    // 被禁言成员
  mutedBy: [],                 // 消息免打扰（复用已有）
  pinnedBy: [],                // 置顶（复用已有）
  lastRead: {},                // 已读标记（复用已有）
  clearedAt: {},               // 清除记录（复用已有）
  maxMembers: 500,             // 最大成员数
  createdAt: '',
  lastMessageAt: ''
}
```

### 2. Message 表扩展
新增消息字段：

```js
{
  // ... 现有字段保持不变
  atUsers: ['user_1', 'all'],  // @提及的用户，'all' 表示 @所有人
}
```

## 二、后端 API 设计

### 1. 群聊管理接口

| 接口 | 方法 | 说明 |
|------|------|------|
| `/api/group-chat/create` | POST | 创建群聊 |
| `/api/group-chat/:id` | GET | 获取群聊详情 |
| `/api/group-chat/:id/update` | POST | 修改群信息（名称、公告等） |
| `/api/group-chat/:id/dismiss` | POST | 解散群聊（仅群主） |
| `/api/group-chat/:id/transfer` | POST | 转让群主 |
| `/api/group-chat/:id/members/add` | POST | 邀请成员 |
| `/api/group-chat/:id/members/remove` | POST | 移除成员 |
| `/api/group-chat/:id/members/leave` | POST | 退出群聊 |
| `/api/group-chat/:id/admins/add` | POST | 设置管理员 |
| `/api/group-chat/:id/admins/remove` | POST | 取消管理员 |
| `/api/group-chat/:id/nickname` | POST | 修改群内昵称 |
| `/api/group-chat/:id/mute-member` | POST | 禁言/解禁成员 |

### 2. 接口详情

**创建群聊** `POST /api/group-chat/create`
```json
{
  "memberIds": ["user_1", "user_2", "user_3"],
  "name": "群聊名称（可选，默认：成员昵称拼接）"
}
```
- 最少选择2个好友（加上自己共3人）
- 自动生成默认群名（前几位成员昵称，如"张三、李四、王五"）

**邀请成员** `POST /api/group-chat/:id/members/add`
```json
{ "memberIds": ["user_4", "user_5"] }
```
- 40人以下：任何成员可邀请
- 40人以上：需群主/管理员审批（当 inviteConfirm=true 时）
- 产生系统消息："xxx 邀请 xxx 加入了群聊"

**移除成员** `POST /api/group-chat/:id/members/remove`
```json
{ "memberId": "user_4" }
```
- 仅群主和管理员可操作
- 管理员不能移除群主和其他管理员
- 产生系统消息："xxx 被移出了群聊"

**退出群聊** `POST /api/group-chat/:id/members/leave`
- 群主退出时必须先转让群主
- 产生系统消息："xxx 退出了群聊"

## 三、实时事件（SSE）

新增/复用的事件类型：

| 事件 | 说明 |
|------|------|
| `message_created` | 复用，增加群消息推送给所有群成员 |
| `conversation_updated` | 复用，群信息变更通知所有成员 |
| `group_member_updated` | 新增，成员变更通知 |

## 四、前端页面设计

### 1. 发起群聊入口
- **聊天列表右上角 "+" 按钮** → 新增"发起群聊"选项
- **联系人页面** → 多选好友后点击"完成"创建群聊
- **现有单聊页面右上角** → "..."菜单中新增"邀请好友，创建群聊"

### 2. 多选好友页面（新建）
- 顶部搜索框，可按昵称/备注搜索好友
- 好友列表带勾选框，已选好友显示在顶部横向滚动区域
- 底部"完成(N)"按钮，显示已选人数
- 样式仿照微信的选择联系人界面

### 3. 群聊对话页面
基于现有聊天页面扩展：

- **标题栏**：显示"群名称(成员数)"，如"工作群(12)"
- **消息气泡**：
  - 每条消息显示发送者头像和昵称（群内昵称 > 好友备注 > 用户昵称）
  - 自己的消息靠右，不显示昵称
  - 系统消息居中灰色显示（入群、退群、改名等）
- **@功能**：
  - 输入框输入"@"弹出成员选择列表
  - 可选择具体成员或"所有人"
  - 被@的消息在列表中高亮显示"[有人@我]"
- **输入区域**：与单聊一致（文字、图片、语音等）

### 4. 群聊设置页面（新建）
从聊天页面右上角"..."进入，仿照微信群设置：

```
┌─────────────────────────────┐
│  群聊信息                    │
├─────────────────────────────┤
│  [头像1] [头像2] [头像3] [+] [-]  ← 成员头像网格
│  查看全部群成员 (12) >       │
├─────────────────────────────┤
│  群聊名称        工作群  >   │
│  群公告          点击查看 >  │
│  我在本群的昵称   未设置  >  │
├─────────────────────────────┤
│  消息免打扰       [开关]     │
│  置顶聊天         [开关]     │
├─────────────────────────────┤
│  清空聊天记录      >         │
│                              │
│  [退出群聊] / [解散群聊]     │ ← 群主看到"解散"，成员看到"退出"
└─────────────────────────────┘
```

### 5. 群成员列表页面（新建）
- 显示所有成员，群主标"群主"标签，管理员标"管理员"标签
- 群主/管理员可长按成员进行操作（移除、设为管理员等）
- 顶部搜索框可搜索群成员

### 6. 会话列表展示
- 群聊会话与单聊混排，按最后消息时间排序
- 群头像显示九宫格拼图（取前9位成员头像）
- 最后消息预览格式："发送者: 消息内容"
- 未读消息数角标
- 被@时显示"[有人@我]"红色标记

## 五、系统消息类型

群聊产生的系统消息：

| 场景 | 消息内容 |
|------|----------|
| 创建群聊 | "xxx 创建了群聊" |
| 邀请成员 | "xxx 邀请 xxx、xxx 加入了群聊" |
| 移除成员 | "xxx 将 xxx 移出了群聊" |
| 成员退出 | "xxx 退出了群聊" |
| 修改群名 | "xxx 修改群名为 \"xxx\"" |
| 发布公告 | "xxx 修改了群公告" |
| 转让群主 | "xxx 将群主转让给了 xxx" |
| 解散群聊 | "群主已解散该群聊" |

## 六、涉及修改的文件

### 后端
| 文件 | 变更 |
|------|------|
| `server.js` | 注册群聊路由 |
| `server_routes_group_chat.js` | **新建** - 群聊 API 路由 |
| `group_chat_service.js` | **新建** - 群聊业务逻辑 |
| `conversation_message_service.js` | 扩展支持群消息（@功能、发送者昵称） |
| `server_index.js` | 新增群聊索引 |
| `server_persistence.js` | 群聊数据持久化 |
| `sqlite_store.js` | 群聊相关 SQL |

### 前端
| 文件 | 变更 |
|------|------|
| `index.html` | 新增群聊相关 UI 模板 |
| `app.js` | 群聊状态管理、路由 |
| `app_chat.js` | 群消息渲染（头像、昵称、@） |
| `app_group_chat.js` | **新建** - 群聊设置、成员管理 UI |
| `app_contacts.js` | 多选好友组件 |
| `app_realtime.js` | 处理群聊实时事件 |

## 七、权限规则总结

| 操作 | 群主 | 管理员 | 普通成员 |
|------|------|--------|----------|
| 修改群名称 | ✅ | ✅ | ✅ |
| 发布群公告 | ✅ | ✅ | ❌ |
| 邀请成员 | ✅ | ✅ | ✅（<40人） |
| 移除成员 | ✅ | ✅（不可移除群主/管理员） | ❌ |
| 设置管理员 | ✅ | ❌ | ❌ |
| 禁言成员 | ✅ | ✅ | ❌ |
| 转让群主 | ✅ | ❌ | ❌ |
| 解散群聊 | ✅ | ❌ | ❌ |
| 退出群聊 | ✅（需先转让） | ✅ | ✅ |
| 修改自己群昵称 | ✅ | ✅ | ✅ |

## 八、不在本次范围内（后续迭代）

- 群聊二维码邀请
- 群聊文件共享
- 群投票功能
- 群接龙
- 群语音/视频通话
- 群消息已读回执（显示谁已读）
