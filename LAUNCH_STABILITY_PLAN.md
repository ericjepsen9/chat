# 上线稳定性修复计划（Web / Android / iOS）

> 目标：在不大改现有主框架的前提下，完成上线前必须的稳定性、数据一致性、权限边界与端侧兼容保障。

## 0. 发布门禁（Go/No-Go）

- P0 全部完成且回归通过。
- 关键链路（注册/登录/聊天/下单/订单状态/上传/SSE）压测与冒烟通过。
- Web、Android WebView、iOS Safari 兼容清单全部勾选。
- 线上监控项可观测（错误率、接口 P95、活跃连接、磁盘增长）。

---

## 1. 风险分级与修复范围

### P0（上线阻断项，必须先修）

1. **数据一致性（SQLite 分支）**
   - 为 `orders` 增加 SQLite schema 和读写映射。
   - 保证 JSON / SQLite 两条存储路径返回结构一致。
   - 增加迁移与启动自检（缺表自动建、版本标记）。

2. **订单并发一致性 + 幂等**
   - 下单、改价、状态变更增加幂等键（requestId / idempotencyKey）。
   - 订单状态机约束：禁止非法回退与重复终态写入。
   - 增加乐观并发控制（version / updatedAt compare）。

3. **持久化失败可观测与失败语义统一**
   - `server_persistence` 不再吞异常：至少记录错误并打指标。
   - 对关键写链路定义“成功响应前最小保证”（例如 WAL append 成功）。
   - 增加健康检查项：最近持久化错误时间、累计失败次数。

4. **权限边界闭环**
   - 增加账号状态位（active / banned），统一在 `requireAuth` 层拦截。
   - 订单动作按角色+状态精细化：买家/卖家动作矩阵。
   - 黑名单/封禁用户在交易和消息链路的边界校验一致化。

### P1（强烈建议上线前完成）

1. **资源治理**
   - `/api/mall`、会话列表、好友列表增加分页参数与最大页大小。
   - 上传增加用户级配额与文件类型校验（含基本魔数/扩展白名单）。
   - WAL 与上传目录增加保留策略（按容量/时间）。

2. **异常恢复与补偿**
   - 订单与聊天卡片写入失败时，落补偿事件并可重放。
   - 前端关键按钮（下单/确认/支付凭证）统一“请求中锁 + 重试防抖”。
   - SSE 断线重连策略加上退避和状态同步兜底。

3. **输入校验基线**
   - 文本字段长度上限与字符清洗统一（备注、广播、商品描述、驳回原因等）。
   - URL 字段协议与长度校验，防止脏数据污染渲染层。

### P2（上线后两周内完成）

1. **前端状态治理**
   - 将 `app.js` 的订单域、会话域、RTC 域拆为独立模块，减少状态串扰。
   - 建立统一事件总线/状态同步规范，减少重复刷新。

2. **技术债收敛**
   - 统一 API 响应结构（`ok/error/code/data`）。
   - 补服务层单元测试模板，新增功能必须带测试。

---

## 2. 多端（Web/Android/iOS）专项验收

## Web（Chrome/Edge/Safari）
- 登录态恢复、登出失效、跨标签页状态一致。
- 上传（图片/语音）在弱网和大文件下表现。
- SSE 断线、切后台恢复、刷新后消息一致性。
- 订单流程全链路（下单/改价/完成）无重复提交。

## Android（WebView）
- 文件选择器、拍照/录音权限流程。
- 输入法回车行为与发送逻辑一致。
- 页面前后台切换后 SSE 自动恢复。
- 长列表滚动性能（聊天、商城、好友）无明显卡顿。

## iOS（Safari / WKWebView）
- EventSource 行为与重连策略稳定。
- 输入焦点、键盘顶起、弹层定位正确。
- 上传/录音兼容性（MIME/扩展名）回归。
- 页面内存增长可控（长会话滚动与切页）。

---

## 3. 测试计划（上线前）

## 自动化

1. **现有 smoke 扩展为 release smoke**
   - 新增：
     - 幂等下单（同 key 两次提交只生成一个订单）。
     - 并发改价/改状态（验证状态机和版本冲突响应）。
     - SQLite 模式下订单持久化读写一致性。
     - SSE token 一次性 + 过期行为。

2. **回归套件分层**
   - API 集成回归：核心接口全覆盖。
   - 前端关键路径 E2E：登录、会话、订单、上传。

## 人工验收

- 每端执行同一份 checklist（功能 + 异常 + 弱网 + 反复点击）。
- 灰度环境对照：新旧版本并行验证 24h。

---

## 4. 发布节奏建议

## T-7 ~ T-5（修复周）
- 完成全部 P0。
- 每天两次回归（中午/晚间）。

## T-4 ~ T-3（稳定周）
- 完成 P1 核心项。
- 压测与容量评估，确认磁盘增长和 P95。

## T-2（预发布）
- 小流量灰度，观察错误率、重试率、订单一致性。

## T-1（发布前冻结）
- 仅允许 blocker 级修复。
- 输出最终 go/no-go 结论。

## T（正式发布）
- 分批放量（10% -> 30% -> 100%）。
- 指标异常触发自动回滚预案。

---

## 5. 监控与回滚

## 必备监控
- API: 5xx 比例、鉴权失败率、订单接口冲突率。
- 实时: SSE 连接数、重连次数、事件投递失败率。
- 存储: WAL 体积、上传目录增长、持久化失败计数。
- 业务: 订单总量 vs 聊天订单卡片数偏差、重复订单率。

## 回滚预案
- 保留前一版本镜像与数据库快照。
- 提供配置开关：
  - 关闭高风险入口（广播/上传可限流）。
  - 降级实时能力（SSE 弱化为轮询）。

---

## 6. 责任分工建议

- 后端：并发一致性、幂等、持久化与权限边界。
- 前端：重复提交防护、状态收敛、兼容性修复。
- 测试：多端回归矩阵与自动化落地。
- 运维：灰度、监控、告警与回滚演练。

---

## 7. 最终上线标准（必须全部满足）

- [ ] P0 全通过。
- [ ] Release smoke 全通过。
- [ ] Web/Android/iOS 验收单全绿。
- [ ] 灰度 24h 无 blocker。
- [ ] 监控与回滚演练完成。

